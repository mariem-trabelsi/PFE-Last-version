package com.foodsafety.repository;

/*
import com.foodsafety.dto.TemporaryExpenseBenDTO;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;



import java.util.List;

@Repository
public interface RequestRepository extends JpaRepository<Request, Long> {
    @Query(value = "SELECT new com.foodsafety.dto.RequestDTO(r.requestId, r.requestStatus, r.totalAmount, r.creationDate, r.typeRequest, r.beneficiaryRequestNumber, r.campaignName, r.campaignStartDay) from Request  r")
    List<RequestDTO> findAllAsDto();
}
*/